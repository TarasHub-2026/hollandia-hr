PORT=3001
CORS_ORIGIN=https://YOUR_USERNAME.github.io
DB_PATH=./data/hr.db

# Optional: Set a secret token to secure the Cognito Forms webhook.
# In Cognito Forms: Settings -> Post JSON Data -> add ?secret=YOUR_SECRET to the URL.
COGNITO_WEBHOOK_SECRET=